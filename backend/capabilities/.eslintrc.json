{
  "extends": [
    "eslint:recommended"
  ],
  "rules": {
    "no-restricted-properties": [
      "error",
      {
        "object": "process",
        "property": "env",
        "message": "Use the centralized env loader from @oxlayer/capabilities-internal/env. Declare env schemas in package env.ts files instead."
      }
    ]
  },
  "overrides": [
    {
      "files": [
        "internal/env/**",
        "**/env.ts"
      ],
      "rules": {
        "no-restricted-properties": "off"
      }
    }
  ]
}
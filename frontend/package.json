{
  "name": "oxlayer-frontend-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --filter './packages/**' dev",
    "build": "bun --filter './packages/**' build",
    "lint": "bun --filter './packages/**' lint",
    "test": "bun --filter './packages/**' test",
    "clean": "bun --filter './packages/**' clean",
    "typecheck": "bun --filter './packages/**' typecheck"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
